<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudentManager"
        tools:targetApi="31">

        <!-- 1. Màn hình chính (Danh sách sinh viên) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 2. Màn hình Thêm sinh viên -->
        <activity
            android:name=".AddStudentActivity"
            android:parentActivityName=".MainActivity"
            android:exported="false" />
        <!-- parentActivityName giúp hiển thị nút Back (<) trên thanh tiêu đề -->

        <!-- 3. Màn hình Chi tiết & Sửa sinh viên -->
        <activity
            android:name=".StudentDetailActivity"
            android:parentActivityName=".MainActivity"
            android:exported="false" />

    </application>

</manifest>